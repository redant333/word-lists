{
  "name": "word-lists",
  "version": "0.0.0",
  "scripts": {
    "all": "jshint . && jasmine --reporter=jasmine-console-reporter && gulp",
    "gulp": "gulp",
    "watch": "gulp watch",
    "spec": "jasmine --reporter=jasmine-console-reporter",
    "lint": "jshint . --verbose"
  },
  "type": "module",
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-file-include": "^2.3.0",
    "gulp-htmlmin": "^5.0.1",
    "gulp-jsonminify": "^1.1.0",
    "jasmine": "^4.2.0",
    "jasmine-console-reporter": "^3.1.0",
    "jshint": "^2.13.4",
    "webpack-stream": "^7.0.0"
  }
}
