{
  "name": "word-lists",
  "version": "0.0.0",
  "scripts": {
    "test": "jasmine --reporter=jasmine-console-reporter && jshint .",
    "spec": "jasmine --reporter=jasmine-console-reporter",
    "lint": "jshint . --verbose"
  },
  "dependencies": {
    "jasmine": "^4.2.0",
    "jasmine-console-reporter": "^3.1.0",
    "jshint": "^2.13.4"
  }
}
